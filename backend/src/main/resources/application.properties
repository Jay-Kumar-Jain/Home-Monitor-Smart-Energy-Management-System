# Server configuration
server.port=8080
spring.main.allow-bean-definition-overriding=true

# Logging
logging.level.org.springframework.web=INFO
logging.level.com.home.monitor=DEBUG

# To test locally, ensure backend runs on your computer IP
# Find your local IP with "ipconfig" and use it in ESP32 code:
# e.g., http://192.168.1.10:8080/api/appliances/update
